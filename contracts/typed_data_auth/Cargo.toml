[package]
name = "soroban-typed-data-auth-contract"
version = "0.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "20.0"

[dev-dependencies]
soroban-sdk = { version = "20.0", features = ["testutils"] }